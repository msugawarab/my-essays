<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            max-width: 680px;
            margin: 40px auto;
            padding: 0 24px;
            /* どのOSでも綺麗に見える「システムフォント」の組み合わせ */
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", "Meiryo", sans-serif;
            line-height: 1.8; /* 行間を少し広めに */
            color: #1a1a1a;   /* 真っ黒より少しだけグレーにすると目が疲れにくい */
            background-color: #fdfdfd;
            -webkit-font-smoothing: antialiased; /* 文字を滑らかにする（Mac用） */
        }
        header { margin-bottom: 50px; }
        footer { margin-top: 50px; font-size: 0.8em; color: #888; }
        a { color: #0000ee; text-decoration: none; }
        a:hover { text-decoration: underline; }
        h1 { font-size: 1.5em; margin-bottom: 0.5em; }
        .post-list { list-style: none; padding: 0; }
        .post-date { color: #777; font-family: sans-serif; font-size: 0.9em; margin-right: 15px; }
    </style>
</head>
<body>
    <header>
        <strong><a href="{{ '/' | url }}">MAKOTO SUGAWARA</a></strong>
    </header>
    
    <main>
        {{ content | safe }}
    </main>

    <footer>
        © {{ page.date.getFullYear() }} MAKOTO SUGAWARA
    </footer>
</body>
</html>